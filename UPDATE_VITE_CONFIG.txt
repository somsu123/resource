🔧 IMPORTANT: Update vite.config.ts for GitHub Pages

Open file: frontend/vite.config.ts

Add this line after line 5:
  base: '/campusshare/',

Your vite.config.ts should look like:

import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  base: '/campusshare/',  // 👈 ADD THIS LINE
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:5000',
        changeOrigin: true,
      },
    },
  },
})

IMPORTANT: Replace 'campusshare' with your actual GitHub repository name!

After updating, run:
git add frontend/vite.config.ts
git commit -m "Configure for GitHub Pages"
git push
